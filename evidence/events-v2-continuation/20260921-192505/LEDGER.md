# Event-v2 continuation ledger

Started: 2026-09-21T19:25:05.7167085+08:00
Authorization: explicit user continuation from residual 222077 in increasing order.

Fixed command options:
```text
solve-events --threads 1 --root-strengthen --probes --root-seconds 120 --seconds 1 --node-limit 1 --prime-chain-steps 50000000 --probe-case-events 1000000 --probe-members 2000 --root-probe-width 8 --probe-events 200000000
```

Each candidate receives a fresh proof and result file, followed by verify-events without external lemmas.

Run state: COMPLETED; later optimized continuation reached 224929 and is paused.

## 222077

`NO`, `VERIFIED_NO`, scope `complete_original_problem`; independent replay passed with exit code 0. Root 19.3607256 s, 7 rounds, 33,713,107 probe events. See `candidates/222077/`.

## 222179

`NO`, `VERIFIED_NO`, scope `complete_original_problem`; independent replay passed with exit code 0. Root 9.1428808 s, 2 rounds, 5,684,231 probe events. See `candidates/222179/`.

## 222209

`NO`, `VERIFIED_NO`, scope `complete_original_problem`; independent replay passed with exit code 0. Root 5.5815758 s, 2 rounds, 6,049,394 probe events. See `candidates/222209/`.

## 222223

`NO`, `VERIFIED_NO`, scope `complete_original_problem`; independent replay passed with exit code 0. Root 32.0714349 s, 5 rounds, 52,019,585 probe events. See `candidates/222223/`.

## 222437

`NO`, `VERIFIED_NO`, scope `complete_original_problem`; independent replay passed with exit code 0. Root 12.8469732 s, 2 rounds, 4,238,287 probe events. See `candidates/222437/`.
